import React, {Component} from 'react';
import { Text, View, ImageBackground } from 'react-native';

export default class Login extends Component {
  return(){
    <ImageBackground source={require('../img/bg-login.jpg')}></ImageBackground>
  }
}